## InkSpirit 0.2.2

### 新功能
- **精灵图动画**：WebGL 纸片起伏效果，静态图片像活物一样呼吸摆动（行走摇摆、开心轻快、睡觉慢呼吸），纯 GPU 渲染几乎不耗 CPU，老电脑也无压力；不支持 WebGL 时自动降级为 CSS 动画
- **双模态自动切换**：自动识别对话类型——任务型问题（代码/分析/方案）自动切专业模式，闲聊自动切陪伴模式；模型辅助判定，识别不准可手动固定（自动/陪伴/专业三档）
- **内容安全审查**：AI 语义级检测（毒品交易、未成年色情、教唆自杀、恐怖内容等直接拒绝），本地模型优先、短消息跳过，兼顾成本与效果

### 修复
- 窗口右边界计算不对称（右侧留空 20px）
- 惯性飞行/挂窗期间被走动动画干扰
- 聊天从设置返回后滚动位置丢失
- 发送消息后自动滚到底部可见
- Live2D 切回精灵图丢失已导入图片
- 精灵图导入入口统一切换模式
- 安全检测绕过（起名注入、短消息绕过、缓存命中绕过）
- 副屏全屏误判、挂窗位置越界
- 设置页打开时隐藏的精灵图动画空转（自动暂停渲染，低配不卡）
- GPU 上下文丢失（睡眠唤醒）导致渲染崩溃
- 精灵图动画卸载竞态（图片加载中切换导致泄漏）
- 主进程/渲染进程异常日志（userData/inkspirit.log），便于排查
- 渲染错误边界：页面出错显示提示而非白屏
