## InkSpirit 0.2.3

### 新功能
- **会话恢复**：重启应用后自动加载最近一次对话，模型继续使用原有上下文，不用从头聊起

### 修复
- **精灵图绿边**：WebGL 纹理改为预乘 Alpha 上传，消除角色边缘的绿色杂边
- **对话滚动**：启动进入聊天时自动滚动到最新消息底部
- **高分屏定位**：修正 125%/150% 等缩放比例下挂窗错位、全屏误判、惯性飞行与拖拽边界（物理像素与 DIP 单位换算）
- **多显示器**：边界按所在显示器工作区原点计算，副屏/负坐标布局不再把宠物挤出屏幕
- **本地→云端回退**：本地模型不可用时回退云端，正确显示实际应答的模型，回复缓存归属正确
- **清除密钥**：设置页将 API Key 留空保存即可删除已存密钥
- **错误提示**：聊天失败时展示具体错误原因，而非笼统的"无法回应"
- **Live2D 稳定性**：共享渲染实例释放竞态修复，避免切换视图时白屏
- **精灵图路径规范化**：头像清理与备份迁移不再因路径格式误删或漏删图片
